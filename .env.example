# Groq API key for the E-Bot chatbot (/api/chat serverless function).
# Get one at https://console.groq.com/keys
# Local dev: copy this file to ".env" and fill in the key, then run `vercel dev`.
# Production: add GROQ_API_KEY in Vercel → Project → Settings → Environment Variables.
GROQ_API_KEY=
